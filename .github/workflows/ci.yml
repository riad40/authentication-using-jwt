name: Marhaba auth CI
on:
  push:
    branches: [ "main" ]
jobs:
  test-api:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./api
    env: 
      PORT: ${{ secrets.PORT }}
      DB_URI: ${{ secrets.DB_URI }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      SERVICE_TRANSPORTER: ${{ secrets.SERVICE_TRANSPORTER }}
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
    steps:
      - uses: actions/checkout@v3
      - name: test api
        uses: actions/setup-node@v3
      - run: npm install
      - run: npm test
  test-fronend:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./frontend
    steps:
    - uses: actions/checkout@v3
    - name: test frontend
      uses: actions/setup-node@v3
    - run: npm install
    - run: npm test
  api-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # fix buildx failed with: ERROR: denied: requested access to the resource is denied
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: push docker image
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/marhaba-auth:${{ github.sha }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          context: ./api
          file: ./api/Dockerfile
  frontend-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: push docker image
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/marhaba-auth-frontend:${{ github.sha }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          context: ./frontend
          file: ./frontend/Dockerfile

