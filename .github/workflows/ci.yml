name: Marhaba auth CI
on:
  push:
    branches: [ "main" ]
jobs:
  test-api:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./api
    env: 
      PORT: ${{ secrets.PORT }}
      DB_URI: ${{ secrets.DB_URI }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      SERVICE_TRANSPORTER: ${{ secrets.SERVICE_TRANSPORTER }}
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
    steps:
      - uses: actions/checkout@v3
      - name: test api
        uses: actions/setup-node@v3
      - run: npm install
      - run: npm test
  test-fronend:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./frontend
    steps:
    - uses: actions/checkout@v3
    - name: test frontend
      uses: actions/setup-node@v3
    - run: npm install
    - run: npm test
  build-api:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./api
    env: 
      PORT: ${{ secrets.PORT }}
      DB_URI: ${{ secrets.DB_URI }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      SERVICE_TRANSPORTER: ${{ secrets.SERVICE_TRANSPORTER }}
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
    steps:
      - uses: actions/checkout@v3
      - name: build api
        uses: actions/setup-node@v3
      - name: push api Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./api
          push: true
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: new02blood/marhaba-auth
          tags: latest

  build-frontend:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./frontend
    steps:
      - uses: actions/checkout@v3
      - name: build frontend
        uses: actions/setup-node@v3
      - name: push frontend Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./frontend
          push: true
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: new02blood/marhaba-auth-frontend
          tags: latest

